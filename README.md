After downloading zip folder
Create and activate a virtual environment:
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   python main.py

then to activate, run these commands
cd CyberEye
source venv/bin/activate
/run.sh
